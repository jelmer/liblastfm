#!/usr/bin/make -f
# -*- makefile -*-

override_dh_auto_configure:
	dh_auto_configure -- -DBUILD_FINGERPRINT=ON -DBUILD_TESTS=OFF $(DEB_CMAKE_CUSTOM_FLAGS)

%:
	dh $@ --list-missing --dbg-package=liblastfm-dbg
